namespace Runtime.UI.Panels.Loading
{
    public enum LoadingPresentation
    {
        /// <summary>App launch — full wheel, letters, sparkles (~2s).</summary>
        Full = 0,

        /// <summary>In-game transitions — wheel + letters, no sparkles (~1.2s).</summary>
        Quick = 1
    }
}
